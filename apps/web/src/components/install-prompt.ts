/**
 * The literal one-shot bootstrap prompt the user copies from the frontpage.
 * Imported by the install-command UI component and by the e2e suite, so
 * any rewording is reflected in both surfaces simultaneously.
 */
export const AGENT_PROMPT =
  "fetch https://dojocho.ai/llms.txt and follow it to install the dojocho CLI in this project";
