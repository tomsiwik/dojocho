.DS_Store
node_modules
.turbo

# Local secrets for e2e tests — see .env.example for the var names.
.env
.env.local

# build outputs
packages/cli/dist/
packages/config/dist/
apps/web/dist/
apps/web/src/routeTree.gen.ts

# Vite + TanStack Start + nitro build artifacts (generated by `pnpm build`).
# Vercel builds these on its own from source — never commit.
.output
.vercel
.tanstack
**/.source

# Runtime artifacts
/logs/
